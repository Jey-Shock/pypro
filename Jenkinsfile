pipeline {
    agent any
    stages {
        stage('Python Version') {
            steps {
                sh 'python3.12 --version'
                sh 'pip3.12 --version'
            }
        }
    }
}